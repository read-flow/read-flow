// Disable SSR and prerendering — all pages are rendered client-side at runtime.
// Data comes from user-configured remote sources, not build-time.
export const ssr = false;
export const prerender = false;
