* Read Flow

This is a tool to help discover and organize files locally and on remote servers.

* Open

** Tier 1 — Quick Wins (High value, Low effort) [5/6]

- [X] ~read-flow~: Allow copying full paths from file details page
- [X] ~read-flow~: Fuzzy search in document list
- [ ] ~read-flow::EpubViewer~: Make nav pane resizeable
- [X] ~read-flow-core~: Make file download folder for HTTP ~client~ configurable
- [X] ~read-flow-core~: Use faster/fastest hash function available for IndexMap, we don't need the security aspects of the default hashing implementation
- [X] ~read-flow-core~ & ~read-flow~: Check files in the database to see whether they still exist
- [X] ~read-flow::EpubViewer~: Support images in data urls
  - log line: 2026-04-06T17:04:24.169257Z  INFO epub::epub::document: resource not found in EPUB archive: OEBPS/html/data:image/png;base64,

** Tier 2 — High Impact, Moderate Effort [6/8]

- [X] ~read-flow~: Fix font rendering when "use theme colors" is enabled in MuPdfViewer on dark themes
  - When theme colors are applied to a PDF that contains real text (not scanned images), the color
    substitution works correctly but fonts appear less sharp than the original — only with dark themes,
    not with light themes. The likely cause is that MuPDF's anti-aliasing blends glyph edges against
    the background color it renders into; when the background is inverted to dark the blended edge
    pixels were computed for a light background and end up the wrong shade. Investigate whether MuPDF
    can re-render with the dark background color as input (so AA is computed correctly from the start)
    rather than inverting a light-background render after the fact.
- [X] ~read-flow~: Bidirectional footnote navigation in EpubViewer
  - Each inline footnote reference in the text should render a named anchor (e.g. =fnref-1-2= for the
    second occurrence of footnote 1). Footnote definitions at the end of the chapter should then link
    back to the specific call-site anchor rather than a single shared target. This ensures that when
    the reader navigates from a footnote back into the text, they return to the exact location of the
    reference they followed — not just the most recently visited one.
- [ ] ~read-flow-core~: HTTP response code & error handling in ~client~ module
- [ ] ~read-flow-core~: Proper HTTP error handling in ~server~ module, including tests
- [X] ~read-flow~: On the Sources page, add an indicator icon for each source whether they are available or not
- [X] ~read-flow~: Allow adding sources that are not available at the time of adding(?) on the Sources page
  - This should be gated with a confirmation dialog if the status of the source cannot be determined
- [X] ~read-flow~: Show scan progress widget in bottom bar while a scan is running
  - The new async scanner (three-stage pipeline: traversal → fingerprinting → DB writer) exposes a
    =tokio::sync::mpsc::Receiver<ScanProgress>= to the caller. The COSMIC app should subscribe to
    this channel and display a live count of discovered/processed files and errors in a bottom bar
    widget. =ScanProgress= events: =FileDiscovered=, =FileProcessed { path, was_new, was_updated }=,
    =FileError { path, error }=, =Completed { discovered, processed, errors }=.
- [X] ~read-flow~: Allow deleting duplicate files (including from file storage) from file edit dialog

** Tier 3 — Significant Features [1/5]

- [-] ~read-flow~: Use context view of Document Details page for multiple actions: [3/4]
  - [ ] Remove duplicates (including files)
    - The ~DeleteSource~ infrastructure is fully implemented (confirmation dialog, actual file deletion via ~delete_document_source~).
    - The delete button currently only appears when the *same* client has ~client_source_count > 1~ copies.
    - For cross-client duplicates (e.g. one local + one remote copy of the same file), no delete button is shown.
    - To complete this: broaden the condition to also show the delete button when sources from *different* clients share the same fingerprint.
  - [X] For missing on local: Pull to local
  - [X] For missing on remote: Push to remote
  - [X] For all (reachable) sources: Check whether the file still exists
- [ ] ~read-flow-core~ & ~read-flow~: Make it possible to correlate `books` of different file types
- [ ] ~read-flow-core~ & ~read-flow~: Per-extension viewer configuration
  - MuPdfViewer supports formats beyond PDF/EPUB/MOBI. Add a config section (or settings page) that
    maps file extensions to viewer backends, so the user can choose how each extension is opened.
    Requires a new =ScanSettings.extensions= field (already designed: data-driven =Vec<String>=
    defaulting to =[pdf, epub, mobi]=) and a corresponding viewer mapping in the UI.
- [ ] ~read-flow-core~ & ~read-flow~: Audit log for automatic file changes during scan
  - The async scanner logs file updates (size/fingerprint changed) and new tag insertions at =INFO=
    level. Future: add an =audit_log= table (migration required) and a COSMIC audit log page where
    the user can inspect what changed between scans and when. The DB writer already has all the
    information needed (old/new size, old/new fingerprint, added tags).
- [X] ~read-flow-core~ & ~read-flow~: Recognize duplicates accross local/remote repositories
- [ ] Separate file discovery in multiple main categories:
  - documents(.doc, .docx, .pdf, .epub, .mobi, etc.)
  - images(.jpg, .png, .webp, etc.)
  - videos(.mpeg, .mov, mp4, etc.)
  - audio(.ogg, .mp3, .wav, etc.)
- [ ] Add unit/integration tests

** Tier 4 — Useful but Larger Scope [1/5]

- [ ] Recognize calibre directories, and support calibre database to import data
- [ ] Allow duplicating files to/from remote, with configuration/input of location.
  - Basic push/pull is already implemented (see Tier 3 checked items): the "Send to Missing" section in Document Details pushes to a remote that lacks the file, and pulls to local when only a remote copy exists.
  - What is *not* implemented is the "with configuration/input of location" part:
    - Downloads always go to the globally-configured ~download_folder~; no per-file destination picker.
    - Uploads send to the remote server with no way to specify the target folder/path on the remote.
    - There is no way to explicitly copy a file to a remote that *already* has it (forced duplicate).
- [ ] Add role-based authorization for server REST API, read/write access
  - [ ] Update cosmic settings editor
- [X] PDF Viewer, 'stream' the pages while loading, so that each page becomes visible/accessible the moment it is rendered.
- [ ] Investigate/use ~pdfium_render~ crate to render PDFs.
- [ ] Add Google drive as document source/storage
- [ ] Add Dropbox as document source/storage
- [ ] Add OneDrive as document source/storage

** Tier 5 — Refactoring / Technical Debt [0/4]

- [X] ~read-flow-core~: modify DAO traits to take the connection as a parameter, so we can add transactions
- [ ] =page/mod.rs= contains a lot of initialization code (and messages) that might need to be handled by =app.rs=
- [ ] ~read-flow~: Use same approach for ~Out~ messages, ~settings.rs~ uses different approach then ~page/mod.rs~
- [X] Change directory modules API so that they can match and still recurse into the directory
  - Resolved by removing =DirectoryModule= entirely; SCM root detection is now inlined in
    =FileSystemVisitor= and the new =Scanner= struct replaces the module abstraction.

** Tier 6 — Low Priority / Niche [0/1]

- [ ] ~read-flow-core~ & ~read-flow~: Implement 2-way TLS (including configuration/database) [0/2]
  - [ ] ~read-flow-core~: Update client and server modules, with configuration
  - [ ] ~read-flow~: Update sources & settings pages to allow configuring the client and server


** Prepare for public release [3/13]

- [X] Move =justfile= from ~cosmic/~ to project root, and update to work for the whole repository
- [X] Create read-flow application branding [1/1]
  - [X] logo
- [ ] Create read-flow website
- [ ] Create working Debian (sid?) package, including Desktop file
  - [ ] Maybe both binaries in the same package
- [ ] Convert org-mode files do markdown files (?)
- [ ] Remove files that are not needed anymore
- [ ] Try to build and run ~read-flow~ on Windows
- [ ] Try to build and run ~read-flow~ on MacBook
- [ ] Licensing [0/2]
  - [ ] Investigate the license of all dependencies
  - [ ] Set license on this repository
- [ ] Extract the ~provider~ crate to a separate repository?
- [ ] Extract the ~epub~ crate to a separate repository?
- [ ] Recreate main ~README.md~
- [X] ~read-flow~: Make first-time setup wizard, when there is no configuration at all.
  - Ensured the application creates default directories and files instead

* Provider Crate — Future Ideas

** Composition & Control Flow

- [ ] ~RetryProvider<P>~: Wraps provider with configurable retry logic (attempts, backoff strategy) for transient failures
- [ ] ~TimeoutProvider<P>~: Adds timeout to async providers - critical for network operations in UI apps
- [ ] ~ZipProvider<P1, P2>~: Combines two providers into a tuple ~(T1, T2)~ - useful when you need data from multiple sources together

** Time-Based Caching

- [ ] ~TtlCache<T, P>~: Time-to-live cache - expires after a duration. Perfect for API responses, thumbnails, metadata
- [ ] ~RefreshingCache<T, P>~: Background refresh before expiration - keeps data fresh without blocking UI
- [ ] ~StaleWhileRevalidate<T, P>~: Returns cached value immediately while fetching fresh data in background - great UX pattern

** File & I/O Providers

- [ ] ~FileProvider~: Reads file content, handles errors gracefully
- [ ] ~WatchingFileProvider~: Uses inotify/notify to auto-invalidate cache when file changes - perfect for config
- [ ] ~CommandProvider~: Executes shell command and provides output - useful for integrating with system tools
- [ ] ~DirectoryProvider~: Lists directory contents with optional filtering

** Network Providers

- [ ] ~HttpProvider~: Fetches data from HTTP endpoints
- [ ] ~ThrottledProvider<P>~: Rate-limits calls to underlying provider - prevents abuse
- [ ] ~OfflineFirstProvider<P>~: Returns cached value when offline, syncs when online

** Cosmic/Desktop-Specific

- [ ] ~CosmicConfigProvider<T>~: Integrates with ~cosmic-config~ for app settings with automatic change detection
- [ ] ~ThumbnailProvider~: Generates/caches thumbnails for files (integrates with freedesktop spec)
- [ ] ~IconProvider~: Provides icons from icon themes based on file type/name
- [ ] ~MimeTypeProvider~: Determines MIME type for files
- [ ] ~XdgProvider~: Provides XDG directory paths (config, cache, data dirs)
- [ ] ~MetadataProvider~: Extracts metadata (EXIF, PDF info, audio tags)

** Reactive/Observable

- [ ] ~StreamProvider<T>~: Produces a stream of values instead of single value - for real-time updates
- [ ] ~PollingProvider<P>~: Periodically polls underlying provider and notifies on changes

** Utility Providers

- [ ] ~LazyProvider<P>~: Defers provider construction until first use
- [ ] ~OnceProvider<P>~: Guarantees provider is called exactly once (like ~OnceCell~ pattern)
- [ ] ~FilterMapProvider<P, F>~: Filters and maps in one step for collection providers
- [ ] ~FlatMapProvider<P, F>~: Maps each item to a provider and flattens results
- [ ] ~OptionProvider<P>~: Gracefully handles ~None~ cases, converts ~Option<T>~ errors

* Completed

- [X] Add error handling to ~serve/mod.rs~ instead of ~unwrap()~
- [X] Make database URL configurable (using ~figment~)
- [X] Add functions to convert between mime-types and extensions
- [X] Use filename from part in multipart form and remove explicit form field
- [X] Store all pages separately in gui, so that they don't need to be reinitialized when switching pages
- [X] Add command to find duplicate entries
- [X] Add configuration/database for remote servers to synchronize with
- [X] Introduce common interface/trait around database and HTTP client
- [X] Add cargo features for rocket server and gui
- [X] Make authorization tokens configurable
- [X] Create page to view/edit file/book/project details
- [X] Add health endpoint, to check whether remote repository is up
- [X] Store configuration in default location like =$HOME/.config/archive-organizer=
- [X] Derive folders to scan from discovered files
- [X] Store folders to scan in database, and add command to scan configured folders
- [X] In File Edit dialog, show all unused tags and allow to set them using a single click
- [X] Open files from the gui
- [X] Order files ascending or descending
- [X] Add private mode, with configuration to hide documents by default
- [X] Allow marking files as read/reading/unread
- [X] Filter files on reading status
- [X] Check if cosmic applications work better on chromeos
  - No, they don't
- [X] Server downloads files into file specific folder: "{download_folder}/{filename}/{filename}.{extension}"
      This way related books, with the same name, but different extension will be stored in the same folder,
      Calibre can then import them as related to the same book.
- [X] Make aggregator fail-safe for remotes that are unavailable at that moment. (use status endpoint)
- [X] Make settings page edit only the file, and apply the commandline arguments for the runtime settings
- [X] Some settings, like the database url, are only effective after a restart, notify the user when they change those
- [X] After adding a source, add the source to the active aggregator (needs Arc<Mutex<Aggregator>>)
- [X] Hash and Salt all passphrases in the `server.authorized_users` settings field
- [X] Update settings editor to also edit the user id
