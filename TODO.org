* Read Flow

This is a tool to help discover and organize files locally and on remote servers.

* Open

** Tier 1 — Quick Wins (High value, Low effort) [2/2]

- [X] ~read-flow::EpubViewer~: Make nav pane resizeable
- [X] ~read-flow::EpubViewer~: Use `widget::search_input` for search box.

** Tier 2 — High Impact, Moderate Effort [5/7]

- [X] ~read-flow-core~: HTTP response code & error handling in ~client~ module
  - Done: ~client.rs~ checks status codes throughout — 401 triggers token refresh, expected 404s map to ~None~, everything else goes through ~error_for_status_ref~.
- [X] ~read-flow-core~: Proper HTTP error handling in ~server~ module, including tests
  - Done: ~impl IntoResponse for Error~ maps domain errors to 400/403/404/500; ~authn~ errors map to 400/401/403. Auth status codes are covered by ~core/tests/oauth.rs~.
- [X] ~read-flow::EpubViewer~: Add progressbar for initial loading.
  - Fixed by speeding up loading
- [X] ~read-flow~: Sync document to all sources simultaneously — ~Aggregator::update_document~ and ~DocumentProvider::update_document~ are implemented but unused. They push the file content to every configured source at once (unlike ~SendToClient~, which targets a single client). Wire these up to a "sync to all remotes" action in the Document Details context pane or library toolbar.
- [X] ~read-flow::OnlineLibrary~: Display cover images in book cards (~cover_url~ is already parsed from OPDS feeds but not yet rendered).
- [X] ~read-flow::OnlineLibrary~: Support paginated OPDS results — fetch and display subsequent pages ("load more") when the feed contains an ~rel="next"~ link.
- [ ] ~read-flow::OnlineLibrary~: Auto-migrate user config when catalog defaults change (e.g. updated search URL for Standard Ebooks) instead of silently falling back to the stale value in ~read-flow.toml~.
  - Moved from Tier 3: scope is small — detect a stored value equal to an old default and replace it with the new default on load.

** Tier 3 — Significant Features [3/3]

- [X] ~read-flow~: Use context view of Document Details page for multiple actions: [4/4]
  - [X] Remove duplicates (including files)
    - Done: in edit mode the Sources list shows a delete button on every source, regardless of which client owns it (cross-client duplicates included), with confirmation dialog and actual file deletion via ~delete_document_source~.
  - [X] For missing on local: Pull to local
  - [X] For missing on remote: Push to remote
  - [X] For all (reachable) sources: Check whether the file still exists
- [X] ~read-flow-core~ & ~read-flow~: Make it possible to correlate `books` of different file types
  - Done: ~merge_documents~ in the DAO merges two documents (metadata + contents keep the winner's identity), exposed through the aggregator/document provider/REST client and as a merge action in the document list UI.
- [X] Add unit/integration tests
  - Done as an ongoing practice: workspace nextest suite (inline ~#[cfg(test)]~ modules + ~core/tests/~), BDD harness (~just bdd~, REST and COSMIC drivers), generated feature-parity matrix, PWA Vitest. TDD is codified in CLAUDE.md.

** Tier 4 — Useful but Larger Scope [1/2]

- [X] Add role-based authorization for server REST API, read/write access [1/1]
  - Done: ~server/authn.rs~ authenticates users (config entries or token claims) with roles; write endpoints require the =owner= role and return 403 ~Forbidden~ otherwise, reads only require authentication.
  - [X] Update cosmic settings editor
    - Done: Preferences has an authorized-users editor (~AuthorizedUserForm~) managing user id, passphrase and roles.
- [ ] Investigate/use ~pdfium_render~ crate to render PDFs.

** Tier 5 — Refactoring / Technical Debt [2/3]

- [ ] =page/mod.rs= contains a lot of initialization code (and messages) that might need to be handled by =app.rs=
- [X] ~read-flow~: Use same approach for ~Out~ messages, ~settings.rs~ uses different approach then ~page/mod.rs~
  - Obsolete: ~settings.rs~ no longer exists — the separate settings pages were merged into the single Preferences page (~page/preferences.rs~), which lives inside the regular page framework.
- [X] ~read-flow::OnlineLibrary~: Remove dead ~SearchFailed~ variant from ~OnlineLibraryMessage~ — it is never constructed (search errors are currently swallowed per-catalog and only surface as empty results).

** Prepare for public release [6/14]

- [X] Move =justfile= from ~cosmic/~ to project root, and update to work for the whole repository
- [X] Create read-flow application branding [1/1]
  - [X] logo
- [ ] Create read-flow website
- [X] Create working Debian (sid?) package, including Desktop file
  - Done: the release workflow builds a .deb via cargo-deb (~just deb~); the package ships =/usr/bin/read-flow=, the .desktop file, metainfo and icon.
  - [X] Maybe both binaries in the same package
    - Resolved: only ~read-flow~ is packaged; ~read-flow-cli~ is an internal test harness, not user-facing.
- [ ] Convert org-mode files to markdown files (?)
  - Remaining org files: =TODO.org=, =REQUIREMENTS.org=.
- [ ] Remove files that are not needed anymore
  - Clarification: sweep the repository for leftover/obsolete files (unused resources, dead scripts, stale docs) before release.
- [ ] Try to build and run ~read-flow~ on Windows
- [X] Try to build and run ~read-flow~ on MacBook
  - Done: the release workflow builds a macOS arm64 .app bundle on =macos-14= (unsigned; see next item).
- [ ] macOS code signing + notarization — the released .app bundle is currently unsigned
- [-] Licensing [1/2]
  - [ ] Investigate the license of all dependencies
    - Clarification: run an automated audit (e.g. ~cargo deny check licenses~ or ~cargo about~) and record the result; =NOTICE= currently only covers the workspace crates and the cosmic-reader derivation.
  - [X] Set license on this repository
    - Done: =LICENSE-GPL= + =LICENSE-MIT= + =NOTICE= at the root; every crate declares ~license~ in its Cargo.toml (core/provider/epub MIT, cosmic/widgets GPL-3.0-or-later).
- [ ] Extract the ~provider~ crate to a separate repository?
- [ ] Extract the ~epub~ crate to a separate repository?
- [X] Recreate main ~README.md~
  - Done as part of the public 0.1.0 release preparation.
- [X] ~read-flow~: Make first-time setup wizard, when there is no configuration at all.
  - Ensured the application creates default directories and files instead

* Provider Crate — Future Ideas

** Composition & Control Flow

- [ ] ~RetryProvider<P>~: Wraps provider with configurable retry logic (attempts, backoff strategy) for transient failures
- [ ] ~TimeoutProvider<P>~: Adds timeout to async providers - critical for network operations in UI apps
- [ ] ~ZipProvider<P1, P2>~: Combines two providers into a tuple ~(T1, T2)~ - useful when you need data from multiple sources together

** Time-Based Caching

- [ ] ~TtlCache<T, P>~: Time-to-live cache - expires after a duration. Perfect for API responses, thumbnails, metadata
- [ ] ~RefreshingCache<T, P>~: Background refresh before expiration - keeps data fresh without blocking UI
- [ ] ~StaleWhileRevalidate<T, P>~: Returns cached value immediately while fetching fresh data in background - great UX pattern

** File & I/O Providers

- [ ] ~FileProvider~: Reads file content, handles errors gracefully
- [ ] ~WatchingFileProvider~: Uses inotify/notify to auto-invalidate cache when file changes - perfect for config
- [ ] ~CommandProvider~: Executes shell command and provides output - useful for integrating with system tools
- [ ] ~DirectoryProvider~: Lists directory contents with optional filtering

** Network Providers

- [ ] ~HttpProvider~: Fetches data from HTTP endpoints
- [ ] ~ThrottledProvider<P>~: Rate-limits calls to underlying provider - prevents abuse
- [ ] ~OfflineFirstProvider<P>~: Returns cached value when offline, syncs when online

** Cosmic/Desktop-Specific

- [ ] ~CosmicConfigProvider<T>~: Integrates with ~cosmic-config~ for app settings with automatic change detection
- [ ] ~ThumbnailProvider~: Generates/caches thumbnails for files (integrates with freedesktop spec)
- [ ] ~IconProvider~: Provides icons from icon themes based on file type/name
- [ ] ~MimeTypeProvider~: Determines MIME type for files
- [ ] ~XdgProvider~: Provides XDG directory paths (config, cache, data dirs)
- [ ] ~MetadataProvider~: Extracts metadata (EXIF, PDF info, audio tags)

** Reactive/Observable

- [ ] ~StreamProvider<T>~: Produces a stream of values instead of single value - for real-time updates
- [ ] ~PollingProvider<P>~: Periodically polls underlying provider and notifies on changes

** Utility Providers

- [ ] ~LazyProvider<P>~: Defers provider construction until first use
- [ ] ~OnceProvider<P>~: Guarantees provider is called exactly once (like ~OnceCell~ pattern)
- [ ] ~FilterMapProvider<P, F>~: Filters and maps in one step for collection providers
- [ ] ~FlatMapProvider<P, F>~: Maps each item to a provider and flattens results
- [ ] ~OptionProvider<P>~: Gracefully handles ~None~ cases, converts ~Option<T>~ errors
