[package]
name = "read-flow"
version = "0.1.0"
edition = "2024"
license = "GPL-2"
description = "Manage Documents Accross Multiple Machines"
repository = "raspberrypi:~/git/archive-organizer.git"
# repository = "https://github.com/peterpaul/read-flow"

authors = ["Peterpaul Klein Haneveld <pp.kleinhaneveld@gmail.com>"]

[package.metadata.deb]
maintainer = "Peterpaul Klein Haneveld <pp.kleinhaneveld@gmail.com>"

[dependencies]
anyhow.workspace = true
async-trait.workspace = true
clap = { workspace = true, features = ["derive"] }
derive_more = { workspace = true, features = ["as_ref", "deref", "from"] }
futures.workspace = true
futures-util.workspace = true
i18n-embed = { workspace = true, features = ["fluent-system", "desktop-requester"] }
i18n-embed-fl.workspace = true
indexmap.workspace = true
mupdf = { workspace = true, features = ["sys-lib"] }
open.workspace = true
rand.workspace = true
rfd.workspace = true
rust-embed.workspace = true
serde = { workspace = true, features = ["derive"] }
thiserror.workspace = true
tokio = { workspace = true, features = ["full"] }
tracing.workspace = true
tracing-subscriber.workspace = true
url.workspace = true

cosmic-text = { git = "https://github.com/pop-os/cosmic-text.git" }

epub = { path = "../epub" }
provider = { path = "../provider" }
read-flow-core = { path = "../core", default-features = false }

[dependencies.libcosmic]
git = "https://github.com/pop-os/libcosmic.git"
# See https://github.com/pop-os/libcosmic/blob/master/Cargo.toml for available features.
features = [
    # Accessibility support
    "a11y",
    # About widget for the app
    "about",
    # Uses cosmic-settings-daemon to watch for config file changes
    "dbus-config",
    # Support creating additional application windows.
    "multi-window",
    # On app startup, focuses an existing instance if the app is already open
    "single-instance",
    # Uses tokio as the executor for the runtime
    "tokio",
    # Windowing support for X11, Windows, Mac, & Redox
    "winit",
    # Add Wayland support to winit
    "wayland",
    # GPU-accelerated rendering
    "wgpu",
]

# Uncomment to test a locally-cloned libcosmic
# [patch.'https://github.com/pop-os/libcosmic']
# libcosmic = { path = "../libcosmic" }
# cosmic-config = { path = "../libcosmic/cosmic-config" }
# cosmic-theme = { path = "../libcosmic/cosmic-theme" }

[dev-dependencies]
golden = { path = "../golden" }
